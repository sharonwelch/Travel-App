Here are the apps currently awaiting review:
<br>
<% @apps_under_review.each do |app| %>
  <%= app.first_name %>
  <%= app.last_name %>
  <br>
  <%= link_to "Review this Application", "/reviewapp/#{app.id}", :class => 'btn btn-primary'%>
  <br>
  <br>
<% end %>
